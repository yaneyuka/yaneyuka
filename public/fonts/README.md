# フォントファイルディレクトリ

このディレクトリには以下のファイルを配置してください：

- `LogotypeGothicCondensed.otf` - カスタムフォント

## フォントについて

元のHTMLで参照されているカスタムフォント `LogotypeGothicCondensed.otf` をこのディレクトリに配置すると、サイト全体でそのフォントが使用されます。

フォントファイルが存在しない場合は、フォールバックフォント（'Hiragino Kaku Gothic ProN', sans-serif）が使用されます。 